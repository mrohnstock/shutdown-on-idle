    var swap = $("#swap").get(0).getContext("2d");
    var swap_data = {
            labels : [{% for stat in system.stats %}"{{ stat.timestamp|date("H:i") }}h"{% if not loop.last %},{% endif %}{% endfor %}],
            datasets : [
                    {
                            fillColor : "rgba(220,220,220,0.5)",
                            strokeColor : "rgba(220,220,220,1)",
                            pointColor : "rgba(220,220,220,1)",
                            pointStrokeColor : "#fff",
                            data : [{% for stat in system.stats %}{{ stat.swap }}{% if not loop.last %},{% endif %}{% endfor %}]
                    }
            ]
    }
    new Chart(swap).Line(swap_data,options);