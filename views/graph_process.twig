    var process = $("#process").get(0).getContext("2d");
    var process_data = {
            labels : [{% for used in system.useds %}"{{ used.timestamp|date("H:i") }}h"{% if not loop.last %},{% endif %}{% endfor %}],
            datasets : [
                    {
                            fillColor : "rgba(220,220,220,0.5)",
                            strokeColor : "rgba(220,220,220,1)",
                            pointColor : "rgba(220,220,220,1)",
                            pointStrokeColor : "#fff",
                            data : [{% for used in system.useds %}{{ used.process }}{% if not loop.last %},{% endif %}{% endfor %}]
                    }
            ]
    }
    new Chart(process).Line(process_data,options);