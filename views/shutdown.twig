<!-- Add-Process-Modal -->
<div id="shutdown" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="label" aria-hidden="true">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3 id="label">Shutdown {{ soi.hostname }}</h3>
    </div>
    <form action="/" method="post" class="form-horizontal">
    <fieldset>
    <div class="modal-body">
        <p>Do you really want to shutdown <strong>{{ soi.hostname }}</strong>?</p>
        <input type="hidden" id="shutdown" name="shutdown" value="{{ soi.hostname }}" />
    </div>
    <div class="modal-footer btn-group" style="padding-left: 220px; padding-right: 220px;">
        <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
        <button style="margin-left: 0px;" type="submit" class="btn btn-danger">Yes</button>
    </div>
    </fieldset>
    </form>
</div>